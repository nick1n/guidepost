name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up pnpm and Node.js
        uses: pnpm/setup@v2
        with:
          runtime: node@24
          cache: true
          install: false

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify InstantDB configuration
        env:
          PUBLIC_INSTANT_APP_ID: ${{ vars.PUBLIC_INSTANT_APP_ID }}
        run: test -n "$PUBLIC_INSTANT_APP_ID" || (echo "::error::Set the PUBLIC_INSTANT_APP_ID repository variable before deploying." && exit 1)

      - name: Check
        run: pnpm check

      - name: Build
        env:
          BASE_PATH: /${{ github.event.repository.name }}
          PUBLIC_INSTANT_APP_ID: ${{ vars.PUBLIC_INSTANT_APP_ID }}
        run: pnpm build

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: build

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
